# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 10 } %>
  timeout: 5000
  default_transaction_mode: immediate

development:
  primary:
    <<: *default
    database: storage/db/development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  primary:
    <<: *default
    database: storage/db/test.sqlite3

performance:
  primary:
    <<: *default
    database: storage/db/performance.sqlite3
    # Hypothesis test: widen page cache and checkpoint window, add busy wait
    # Stock is cache_size 2000 (8MB), wal_autocheckpoint 1000 (4MB), busy_timeout 0.
    # This branch tests whether the knee at 100→200 is the single-writer queue.
    variables:
      cache_size: -64000
      wal_autocheckpoint: 4000
      busy_timeout: 5000
      journal_mode: wal
      synchronous: normal
      temp_store: memory

production:
  primary:
    <<: *default
    database: storage/db/production.sqlite3
